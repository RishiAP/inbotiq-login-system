## Frontend environment variables
## Set NEXT_PUBLIC_API_URL to the backend URL (include protocol and port if local)
## Example: NEXT_PUBLIC_API_URL=http://localhost:4000
NEXT_PUBLIC_API_URL=http://localhost:4000

## Optional: name of the auth cookie the backend sets (defaults to 'token')
## This is informational only; the cookie is HttpOnly and not readable from JS.
NEXT_PUBLIC_AUTH_COOKIE_NAME=access_token

## Development frontend URL (used by backend CORS configuration)
## Example: NEXT_PUBLIC_FRONTEND_URL=http://localhost:3000
NEXT_PUBLIC_FRONTEND_URL=http://localhost:3000