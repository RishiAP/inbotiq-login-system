## Backend environment example
## Replace the values below with your real configuration before running the server.

# MongoDB connection string (MongoDB Atlas example)
MONGO_URI=mongodb+srv://<username>:<password>@cluster0.example.mongodb.net/mydb?retryWrites=true&w=majority

# JWT secret used to sign authentication tokens. Use a long, random value in production.
JWT_SECRET=replace_this_with_a_strong_secret

# The port the backend listens on
PORT=4000

# The frontend origin used for CORS (e.g. http://localhost:3000)
FRONTEND_URL=http://localhost:3000

# Optional: name of the auth cookie set by the server. Keep default 'token' unless you change code.
AUTH_COOKIE_NAME=access_token